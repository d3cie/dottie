version: "2"
sql:
  - engine: sqlite
    schema: migrations/sqlite
    queries: internal/infra/sqlite/queries
    gen:
      go:
        package: db
        out: internal/infra/sqlite/db
        emit_json_tags: true
        emit_interface: true
        emit_empty_slices: true

